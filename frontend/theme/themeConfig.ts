import type { ThemeConfig } from 'antd'

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: '#1677ff',
  },
  components: {
    Layout: {
      headerBg: '#fff',
      headerHeight: 64,
      siderBg: '#fff',
    },
  },
}

export default theme 